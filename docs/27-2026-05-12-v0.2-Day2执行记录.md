# 2026-05-12 v0.2 Day 2 执行记录

## 今日目标

完成后端与数据库基础：计划-素材绑定表、批量创建计划接口、项目计划工作台接口、AI usage 基础表。

## 已完成

- 新增 `campaign_materials` ORM、schema、repository。
- 新增 migration：
  - `backend/alembic/versions/20260512_001_add_campaign_materials_and_ai_usage.py`
- 新增计划素材绑定接口：
  - `GET /api/v1/campaigns/{campaign_id}/materials`
  - `POST /api/v1/campaigns/{campaign_id}/materials`
  - `PUT /api/v1/campaigns/{campaign_id}/materials/{binding_id}`
  - `DELETE /api/v1/campaigns/{campaign_id}/materials/{binding_id}`
- 保留旧接口兼容：
  - `POST /api/v1/campaigns/{campaign_id}/materials/{material_id}`
  - `DELETE /api/v1/campaigns/{campaign_id}/materials/{material_id}`
- 新增项目计划工作台接口：
  - `GET /api/v1/projects/{project_id}/plan-workspace`
- 新增批量创建计划接口：
  - `POST /api/v1/projects/{project_id}/campaigns/batch`
- 新增 AI usage/output/budget 数据模型。
- 已执行本地 migration：
  - `venv/bin/alembic upgrade head`

## 真实数据测试

测试项目：

- `fa977103-eae9-4484-b5bc-0d454340e8fc`

测试素材：

- `f49255f1-bd4d-483e-8c40-030d603cbb6a`

测试账户：

- `9924b13d-c6f2-4a77-bf61-2a07a4cc5f11`

已验证：

- `GET /health` 通过。
- `GET /api/v1/projects` 通过。
- `GET /api/v1/materials` 通过。
- `GET /api/v1/platform/accounts` 通过。
- `GET /api/v1/projects/{project_id}/plan-workspace` 通过。
- `POST /api/v1/projects/{project_id}/campaigns/batch` 创建成功。
- `GET /api/v1/campaigns/{campaign_id}/materials` 能返回绑定标题、描述、文案和素材本体。
- `PUT /api/v1/campaigns/{campaign_id}/materials/{binding_id}` 能更新绑定元数据。
- `POST /api/v1/ai/run` 能返回 mock AI 输出并写 usage log。

## 新增测试数据

- 批量创建计划：
  - `804dd4d1-e69e-4149-8819-71086bec3043`
  - 名称：`Day2_Meta_Test_1`
- 计划素材绑定：
  - `5462f2dd-c1d2-44f7-83c3-60fdf75b7023`

## 发现断点

- 旧计划只有 `material_ids`，没有绑定表详情；workspace 会显示绑定数量，但旧数据的标题/描述为空。
- Day 3 前端必须兼容两类素材结构。
- Pydantic `copy` 字段警告已修复为内部 `ad_copy`、外部 alias `copy`。

## 验证结果

- `python3 -m compileall backend/app` 通过。
- `pnpm run build` 通过。

