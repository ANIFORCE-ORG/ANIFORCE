# 2026-05-12 v0.2 Day 1 执行记录

## 今日目标

冻结项目-计划-素材层级、API 合约、AI Gateway 入口和 Token 成本模型，为后续 6 天连续开发提供统一边界。

## 已完成

- 已上传当前 v0.1 客户交付迭代回退点：
  - 本地提交：`0369358 chore: 保存v0.1客户交付迭代回退点`
  - 远端备份分支：`origin/backup/v0.1-customer-delivery-20260512`
- 已确认 `origin/master` 有远端新提交，未强推主分支。
- 已验证回退点：
  - `pnpm run build` 通过。
  - `python3 -m compileall backend/app` 通过。
- 已完成并行只读分析：
  - 后端数据/API 差异。
  - 前端项目计划流程差异。
  - AI Gateway/Chat 现状。
- 已新增 Day 1 合约文档：
  - `docs/25-v0.2-Day1-API合约与数据模型冻结.md`
- 已新增后端数据骨架：
  - `CampaignMaterial` 模型。
  - `campaign_materials` schema。
  - SQLite repository。
  - Alembic migration。
- 已新增 AI Gateway P0 骨架：
  - `/api/v1/ai/run`
  - 场景白名单。
  - Mock output。
  - Token 预估。
  - 日 token 预算。
  - `ai_usage_logs` 和 `ai_outputs` 落库模型。

## 今日判断

- 计划-素材绑定表必须作为准数据源。
- 旧 `material_ids/project_ids/campaign_ids` 只保留兼容，后续新接口同步维护。
- 前端最大业务断点是素材选择没有标题/描述绑定。
- Meta 创建分支当前会丢失已选素材，Day 3 必须修复。
- AI 当前只有 mock chat，v0.2 必须改为后端 AI Gateway。

## 待验证

- 后端编译。
- 前端构建。
- 新增模型与 Alembic import。
- `/api/v1/ai/run` 冒烟。

## Day 2 入口

继续完成：

1. CampaignMaterial API CRUD。
2. 批量创建计划接口。
3. 项目计划工作台聚合接口。
4. API 冒烟和记录。

