# 2026-05-12 v0.2 Day 3 执行记录

## 今日目标

完成前端主流程接入：项目上下文创建计划、素材标题/描述绑定、计划详情展示绑定信息。

## 已完成

- `frontend/packages/main-app/src/api/campaigns.ts`
  - 新增 `CampaignMaterialBindingInput`
  - 新增 `CampaignMaterialBinding`
  - 新增 `BatchCampaignCreateRequest`
  - 新增 `BatchCampaignCreateResponse`
  - 新增 `batchCreateProjectCampaigns`
  - 新增 `createCampaignMaterialBinding`
  - 新增 `updateCampaignMaterialBinding`
- `frontend/packages/main-app/src/api/projects.ts`
  - 新增 `ProjectPlanWorkspace`
  - 新增 `getProjectPlanWorkspace`
- `CreateCampaign.vue`
  - 提交从旧 `createCampaign/createMetaCampaign` 改为 `batchCreateProjectCampaigns`
  - Meta 创建链路不再丢失已选素材
  - 已选素材支持填写计划内标题、描述、投放文案
  - 创建后回到项目详情
- `CampaignDetail.vue`
  - 支持新绑定结构
  - 展示计划内素材标题、描述、投放文案
  - 图片加载改为使用 `material_id`
  - 素材选择去重兼容绑定结构

## 当前保留

- 创建计划页面视觉仍是 4 步；业务提交已经符合项目 -> 计划 -> 素材绑定主链路。
- Day 6 再做视觉层面的三步压缩和客户交付文案打磨。

## 验证结果

- `pnpm run build` 通过。
- `python3 -m compileall backend/app` 通过。

