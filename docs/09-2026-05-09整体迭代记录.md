# 2026-05-09 整体迭代记录

## 迭代目标

本次迭代围绕广告投放 Agent 系统从 Demo 流程走向真实生产流程，重点补齐 Meta 平台连接、真实 Campaign 创建、广告账户管理、素材与报表页面恢复，以及项目-广告账户-Agent 行动队列的核心链路。

## 主要完成内容

### 1. Meta 平台连接与真实 Campaign 创建

- 将 Meta 平台连接从简单 Token 输入，调整为更接近生产环境的 OAuth 配置流程。
- 在系统设置中提供平台连接入口，支持 Meta / Google / TikTok 的平台分组展示；当前 Meta 已实现核心流程。
- 后端新增/完善 Meta OAuth 配置保存、授权 URL 生成、回调处理、账号导入和状态同步能力。
- 接入 Meta Graph API，支持通过系统内的广告创建流程真实创建 Meta Campaign。
- Campaign 创建成功后，本地记录写入真实平台返回的 Campaign ID，便于在 Meta 后台核对。

已验证的 Meta 沙盒信息：

- Meta 广告账户：`act_1943996592887453`
- 已创建 Meta Campaign ID：`6948925380092`
- 已创建 Meta Campaign ID：`6948950991292`

### 2. 广告账户与媒介运营整合

- 将“媒介运营”能力合并到“广告账户”栏目，作为广告账户首页。
- 广告账户管理保留开户、充值、清零、绑定、回收等账户操作能力。
- 广告账户操作页面增加同步状态展示。
- 广告账户操作页面增加操作历史弹窗，便于追踪账户生命周期动作。
- 平台连接入口从一级菜单移入“设置”，避免和广告账户管理职责混淆。

### 3. 创建广告计划流程优化

- 创建广告计划步骤切换时自动滚动到页面顶部，减少操作跳转后的错位。
- 广告创建流程接入项目已绑定广告账户。
- 新建 Campaign 时，优先展示项目已绑定的平台广告账户。
- 若项目暂未绑定账户，保留已有账户兜底选择，避免历史测试流程被阻塞。
- 创建真实 Meta Campaign 后，自动将对应广告账户绑定到项目。

### 4. Campaign 数据结构升级

Campaign 表新增一等字段，减少平台数据长期藏在 `config` 中导致诊断和同步困难：

- `platform_account_id`
- `external_campaign_id`
- `external_status`
- `objective`
- `budget_type`
- `daily_budget`
- `lifetime_budget`
- `bid_strategy`
- `last_synced_at`
- `last_sync_error`

兼容逻辑：

- 对旧数据继续从 `config.platform_account_id` 和 `config.remote_campaign_id` 回退读取。
- 已将当前 SQLite 中 2 条旧 Meta Campaign 记录回填到新字段。
- Campaign 列表、表格、详情页展示平台账户和外部 Campaign ID。

### 5. 项目-广告账户-Agent 权限与执行链路

新增项目与广告账户绑定模型：

- `ProjectPlatformAccount`

新增最小版 Agent 行动模型：

- `AgentAction`

项目详情页新增：

- 关联广告账户区域
- 可绑定账户下拉选择
- 已绑定账户卡片
- 解绑操作
- Agent 行动队列
- 生成建议动作
- 确认 / 拒绝建议动作

当前 Agent 行动队列最小规则：

- 项目没有绑定广告账户时，建议绑定账户。
- Campaign 预算使用率超过 90% 时，建议检查预算。
- Campaign 缺少平台广告账户时，建议补充账户绑定。

### 6. 素材与报表页面恢复

- 迁移并恢复创意素材相关功能。
- 修复广告计划列表中“添加素材”按钮无响应的问题。
- 恢复数据报表页面，避免路由进入后空白。
- 补齐前端素材 API、素材关联 Campaign 的交互逻辑。

### 7. 数据库迁移

新增迁移文件：

- `20260509_001_add_material_legacy_performance_fields.py`
- `20260509_002_add_platform_accounts.py`
- `20260509_003_agent_account_campaign_linkage.py`

本地数据库迁移状态：

- Alembic 已升级到 `20260509_003`。
- 修复了本地 `alembic_version` 曾指向不存在 revision `20260506_001` 的问题。

## 验证结果

已完成验证：

- 后端应用 import 正常。
- Alembic 迁移执行成功。
- 前端生产构建通过：`npm exec vite build`。
- 后端健康检查通过：`GET /health`。
- Campaign 列表能返回 `platform_account_id` 和 `external_campaign_id`。
- 项目广告账户绑定接口可用。
- Agent 行动队列接口可用。
- 项目绑定 Meta 沙盒账户后，重新生成 Agent 行动队列返回空数组，说明当前账户链路无缺口。
- 前端本地服务可访问：`http://127.0.0.1:3011/`。
- 后端本地服务可访问：`http://127.0.0.1:8010/`。

## 当前本地服务

- 前端：`http://127.0.0.1:3011/`
- 后端：`http://127.0.0.1:8010/`
- 后端健康检查：`http://127.0.0.1:8010/health`

## 仍需注意的问题

- 当前平台连接中 Meta 已进入可测试状态，Google / TikTok 仍是平台分组与后续扩展入口。
- 当前 Agent 行动队列是最小规则版，尚未接入完整诊断、执行、回溯评估闭环。
- Campaign 创建目前已能真实创建 Meta Campaign，但 Ad Set、Creative、Ad 的完整链路仍需继续开发。
- 本地 SQLite 数据库属于开发数据，不建议作为生产数据源提交或依赖。
- Meta OAuth 生产环境仍需要稳定 HTTPS 域名、正确的 Valid OAuth Redirect URIs 和 App Domains 配置。

## 下一步建议

1. 补齐 Meta Campaign 后续层级：Ad Set、Creative、Ad。
2. 将 Agent 行动队列升级为“建议-确认-执行-结果回写-效果评估”闭环。
3. 将项目广告账户绑定规则接入真实组织、成员、投手权限。
4. 为平台账户增加定时同步任务，回写账户状态、消耗、余额和外部 Campaign 状态。
5. 增加端到端测试用例，覆盖平台连接、项目绑定账户、真实 Campaign 创建、素材绑定和行动队列。
6. 逐步将 Demo/沙盒数据与生产数据隔离，形成明确的环境切换策略。
