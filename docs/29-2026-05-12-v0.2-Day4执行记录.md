# 2026-05-12 v0.2 Day 4 执行记录

## 今日目标

完成 AI Gateway P0：统一模型调用入口、场景白名单、Token/Cost usage log、预算入口和前端模型 API 入口。

## 已完成

- 后端新增：
  - `POST /api/v1/ai/run`
  - `GET /api/v1/ai/usage/summary`
  - `GET /api/v1/ai/usage/logs`
  - `POST /api/v1/ai/usage/budget`
- 新增 AI 场景控制：
  - `project_draft`
  - `plan_extract`
  - `material_recommend`
  - `material_copy`
  - `plan_review`
  - `campaign_diagnosis`
  - `report_summary`
  - `chat_general`
- 新增 AI Gateway 服务：
  - 场景白名单。
  - 输入 token 预估。
  - 场景输入/输出上限。
  - 用户日 token 预算。
  - Mock Provider 输出。
  - usage log 和 output 落库。
- 前端新增：
  - `frontend/packages/main-app/src/api/ai.ts`
  - `runAI`
  - `getAIUsageSummary`
  - `getAIUsageLogs`
  - `setAIUsageBudget`

## 冒烟测试

已验证：

- `POST /api/v1/ai/run`，场景 `material_copy`，成功生成候选标题/描述并写 usage。
- `GET /api/v1/ai/usage/summary`，返回 token 汇总。
- `GET /api/v1/ai/usage/logs`，返回调用日志。
- `POST /api/v1/ai/usage/budget`，成功设置用户预算。
- `POST /api/v1/ai/run`，场景 `campaign_diagnosis`，返回诊断建议。

## 输出约束

- 当前 AI 输出为 mock JSON。
- 输出状态为 `draft/suggested`。
- `requires_human_confirm` 默认为 true。
- 未接入真实 OpenAI Provider，后续只替换 provider 层，不改业务入口。

## 验证结果

- `python3 -m compileall backend/app` 通过。
- `pnpm run build` 通过。

