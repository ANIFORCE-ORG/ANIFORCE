# 2026-05-12 v0.3 Day 1 执行记录

## 今日目标

进入 P1/P2 下一阶段，优先落地 P1：

- 右侧助手接入 AI Gateway。
- 素材标题/描述生成 3 个候选。
- Settings 内嵌 AI 使用量看板。

## 已完成

- 新增 v0.3 P1/P2 开发计划：
  - `docs/36-v0.3-P1P2-AI协作与策略生成开发计划.md`
- 新增前端 AI 聊天 helper：
  - `frontend/packages/main-app/src/utils/aiAssistant.ts`
- ProjectDetail 右侧助手接入 AI Gateway：
  - 根据输入选择 `material_recommend` / `plan_extract` / `chat_general`
  - 传入项目、预算、计划、账户上下文
  - 消息展示 token 消耗
- CampaignDetail 右侧助手接入 AI Gateway：
  - 根据输入选择 `report_summary` / `campaign_diagnosis` / `chat_general`
  - 传入计划和素材绑定上下文
- WorkspaceDashboard 右侧助手接入 AI Gateway：
  - 调用 `chat_general`
- CreateCampaign 素材绑定区新增：
  - “AI 生成”按钮
  - 每个素材 3 个标题/描述候选
  - “应用”候选到表单
  - 应用后 source 标记为 `ai`
- 后端 `material_copy` mock 改为稳定返回 3 个候选。
- Settings 新增 AI 使用量卡片和内嵌看板：
  - 总 token
  - 预估成本
  - 按 scenario 汇总
  - 最近调用日志
  - 日 token 限额设置

## 验证

- `python3 -m compileall backend/app` 通过。
- `pnpm run build` 通过。
- `POST /api/v1/ai/run` `material_copy` 返回 3 个候选。
- `POST /api/v1/ai/run` `chat_general` 正常返回。
- `GET /api/v1/ai/usage/summary` 能读到新增调用。
- `GET /api/v1/ai/usage/logs` 能读到新增调用日志。

## 当前限制

- AI 仍为 mock provider。
- ChatPanel 暂未展示单独 loading 状态，仅用“正在分析...”消息占位。
- Settings 看板为轻量内嵌版本，还没有分页和筛选 UI。

## 下一步

v0.3 Day 2：

- 复制计划进入确认流程。
- 或继续 P2：真实 OpenAI Provider provider 层接入。

