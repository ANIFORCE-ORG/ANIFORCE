{
  "name": "animagus-demo",
  "version": "0.1.0",
  "private": true,
  "description": "AD Agent Demo - 微前端架构",
  "scripts": {
    "dev": "pnpm --filter main-app dev",
    "dev:all": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
